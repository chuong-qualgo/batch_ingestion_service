# Core
pytest==8.2.0
pytest-mock==3.14.0

# Mocking AWS
boto3==1.34.0
moto[sqs]==5.0.7

# Redis mock
fakeredis==2.23.0
redis==5.0.4

# PySpark (lightweight for unit tests)
pyspark==3.5.1

# Airflow (minimal)
apache-airflow==2.9.1
apache-airflow-providers-common-sql==1.14.0

# OpenBao / Vault client
hvac==2.1.0

# MongoDB
pymongo==4.7.0
mongomock==4.1.2

# YAML
PyYAML==6.0.1

# Cassandra driver (mocked in tests)
cassandra-driver==3.29.0
